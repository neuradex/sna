# About SNA - 클로드코드와 코덱스를 백엔드 런타임으로!

나와 같이 스타트업을 준비하는 일본인 동료가 멋진 프롬프트를 만들어서 본인의 업무 효율을 극대화 시키고 있다는 자랑을 들었다.
이 친구는 영업직인데 여러가지 방법을 동원해서 아웃바운드 영업을 한다.

이 친구가 만들었다고 나에게 자랑한걸 들어보니까, 세개 정도의 프롬프트를 셋팅해서 그걸 순서대로 클로드에 붙여넣으면서 업무를 많이 자동화했다고 자랑을 했다.

대충 다음과 같은 파이프라인이었다.
1. 먼저 본인이 보내고 싶은 메세지와 내용을 사전에 입력해둠
2. 영업 타겟을 자연어로 설명해서 그것에 부합하는 회사 공식홈페이지를 리스트업 시킴
3. 그 회사에 문의 폼 페이지를 찾아내서 그 폼에 입력할 내용을 자동으로 완성 시킴
4. 그 다음 본인이 그 문의 페이지로 들어가서 복사 붙여넣기로 직접 송신 (..)

인공지능을 이용해서 갖은 개발을 하는 내가 보기에는 다소 조촐한 워크플로우였지만 얘는 굉장히 기뻐하고 있었다. 그런데 자기가 뭘 효율적으로 제대로 못하고 있는건지 이거를 15번 정도 하면 클로드 토큰이 바닥난다고 토로하고있었다.

그래서 그 친구에게 물었다. "그거 만약에 실제로 폼 입력이랑, 자동 송신까지 되고, 15번이 아니라 30번 정도까지 자동으로 되는걸 제품으로 만들면 어떨거같아? 영업직들 사이에서는 잘 팔릴거같아?"
라고 물으니 망설임도 없이 고개를 끄덕였다.

그래서 그냥 무턱대고 프로젝트를 시작했다.
처음 내 생각은 이랬다. '클로드코드 스킬이 있으니까, 얘가 만든 프롬프트를 좀더 최적화해서 스킬로 만들고, 데이터는 Sqlite에 저장을 시키고, 버튼을 클릭할때마다 Xterm으로 띄운 클로드 코드에 `/scrape` `/get-form` `/fill-form` `/send` 입력을 시키고, playwright도 브라우저 띄워서 송신시키면 끝이겠네.'

그렇게 시작했다. 그리고 앱을 만들기 시작했다. 얘를 기쁘게 해줘야겠다는 순수한 마음으로 시작했다.
Electron + Xterm 터미널 + 그리고 UI를 붙여서 클릭을 하면 xterm에 커맨드가 송신되는 구조였다.
UI상에 Progress를 표시하기 위해 스킬 중간 중간에 진척보고를 하는 CLI를 두드리게 해놨다.

실제로 내 예상대로 작동하기 시작했다.
영업 타겟을 입력하고 송신을 누르면 Xterm위에 `/scrate {입력한것}`같은 식으로 입력이 되었고 실제로 클로드 코드 스킬이 실행되었고, sqlite에 데이터 출력까지.

복잡한 스키마 고정은 sqlite의 테이블구조가 강제해줬기 때문에 레코드 INSERT에 실패하면 클로드 코드가 알아서 '아 잘못했구나' 하고 올바른 구조로 다시 INSERT한다.

아주 만족스러웠다. 나는 그리고 여기에 Skills-native app이라는 멋진 이름을 붙였다.

그런데.. 하다보니까 조금 마음에 안드는 구석이 발견되었다. 바로 진행상황을 받아오는게 폴링이라 실시간성이 떨어고, 동시에 여러개의 처리를 돌리지 못한다는 점이었다. 한개의 클로드 코드에다가 연속해서 skill을 치고 있으니 그건 당연했다.

그래서 좋은 방법이 없을까? 하다가 고민하다 보니까 '클로드 코드를 그냥 서브 프로세스로 아예 spawn해서 스킬을 돌리게 하면 Xterm을 띄우지 않아도 그냥 백엔드 서버처럼 이용할 수 있잖아?' 라는 깨닫음을 얻게 된다.

돌이켜보면, 이때부터 영업친구를 기쁘게 해주겠다는 순수한 목적은 잊었고 나와의 싸움이 시작되고 있었다.

이 다음에도 마음에 안드는 것이 자꾸 자꾸 쌓이게 되어서 결국에 꽤나 거대한 라이브러리만드는데에 다다르게 되었다. Skills-native App 이라고 이름 붙였는데, 스킬은 온데간데 없고 그냥 코딩 에이전트 그 자체를 서버로 이용하는 거대한 라이브러리가 되었다.

오랜 싸움 끝에 SNA가 탄생하게 되었다.

# SNA

결과적으로, 컨슈머 어플리케이션에서 클로드코드와 코덱스를 런타임 엔진으로 이용할 수 있도록 HTTP/WS 서버로 랩핑해서 로컬에서 돌아가는 라이브러리 만들었다. HTTP 서버는 전부터 써보고 싶었던 hono로 만들었다.

게다가 멀티 패키지로..
- `@sna/core`: HTTP/WS 서버
- `@sna/client`: Typescript SDK
- `@sna/react`: 간단한 채팅 컴포넌트가 들어있는 패키지
- `@sna/testing`: Anthropic Mock Server

대충 `POST /agent/create` 를 하면 옵션에 따라서 Claude Code 프로세스나 Codex 프로세스가 백그라운드에서 Spawn 되고, `POST /sessions/:id/messages` 를 실행하면 해당 프로세스에다 메세지를 밀어넣는식이다. 그리고 Client패키지에서 subscribe()하면 웹소켓 경유로 리얼타임으로 메세지가 흘러들어오는 식이다(Firebase의 그것과 비슷하다)

## 대화이력 추상화

Claude Code/Codex를 병렬로 Spawn해서 서브프로세스로 이용한다. SNA에서 대화 이력은 Claude Code에도 Codex에도 귀속되지 않는다. 로컬 sqlite에 SNA 자신의 정규화된 포맷으로 전부 저장하고, 대화를 재개할 때 그 포맷을 각 프로바이더의 네이티브 형식으로 다시 변환해서 먹인다. 그래서 클로드 코드에서 채팅을 하다가 중단되고 코덱스에서 재개해도 이력이 그대로 이어진다. 모델을 도중에 바꾸는 것도 같은 원리.

이 정규화 포맷이 한참 고민하다 정착시킨 부분이다. 클로드 코드는 메세지 안에 content 배열을 끼워넣는 Anthropic 형식이고, 코덱스는 또 자기네 ResponseItem 배열이고, 거기다 이미지·파일·thinking·tool_use·tool_result가 다 다르게 굴러간다. 둘의 교집합만 갖고 가면 표현력이 부족하고, 합집합을 갖고 가면 변환할 때마다 손실이 난다.

그래서 SNA는 한 메세지를 여러 행으로 잘게 쪼개서 저장한다. `actor`(누가: user / assistant / system)와 `kind`(뭘: text / thinking / tool_use / tool_result / status / error)를 직교 축으로 분리하고, 이미지·파일은 `embeds` JSON에 따로 두고 본문에는 `![](embed://<id>)` 같은 인라인 참조만 남긴다. 이러면 스트리밍 친화적이고, 쿼리하기 좋고, 어떤 프로바이더로도 변환할 수 있다.

여기에 3-layer attribution을 추가했다. `provider`(런타임: claude-code / codex), `modelProvider`(모델 벤더: anthropic / openai / google), `model`(슬러그: sonnet-4-6 / gpt-5.4) 세 축을 분리했다. 이건 나중에 OpenCode를 지원하거나, OpenRouter등 런타임에 다른 프로바이더 모델을 집어넣을 경우를 대비해서 미리 나눠놨다.

## Hooks / MCP / Policy 등 설정 추상화

이 또한 마찬가지로 어떤 프로바이더에게도 귀속되지 않는 형태로 정의를 가능하게 해놓아, 세션 도중에 모델이나 프로바이더가 변경되어도 같은내용의 설정을 이용하는 상태로 세션을 이어갈 수 있다. 나는 클로드 코드만의 설정, 코덱스만의 설정을 따로 따로 하기가 너무 귀찮아서 이 부분도 추상화를 시키고, 각 프로바이더별로 Adaptor를 구현했다.

## Typescript SDK

`@sna/client` 라는 패키지에서는 웹소켓을 적극적으로 이용해서 메세지 송수신 이벤트, 세션 라이프사이클 이벤트, 퍼미션 훅 등을 리얼타임으로 구독할 수 있도록 만들었다.

## 멀티 세션

`POST /agent/create`를 여러 번 부르면 그만큼의 클로드 코드/코덱스 프로세스가 백그라운드에서 동시에 굴러간다. 각 세션은 본인의 이벤트 버퍼와 cwd, 메타데이터를 가지고 있어서 같은 SNA 서버를 여러 앱이 공유해도 `meta.app` 같은 키로 자기 세션만 골라낼 수 있다. WebSocket 커넥션 하나로 여러 세션을 동시에 subscribe할 수 있어, 화면 한쪽에서는 영업 메일 초안을 짜고 다른 한쪽에서는 그 회사 정보를 리서치하는 식의 병렬 워크플로우가 그제서야 가능해진다.

## 퍼미션 플로우 추상화

클로드 코드는 PreToolUse 훅으로, 코덱스는 JSON-RPC 양방향 승인으로, 두 프로바이더가 권한 승인을 처리하는 방식이 완전히 다르다. SNA는 이 차이를 컨슈머 앱이 알 필요 없게 막았다.

에이전트가 Bash든 Edit든 위험 도구를 부르려 하면, SDK가 미리 주입해둔 훅이 SNA 서버에 "이거 해도 돼?" 하고 묻고 UI 응답이 올 때까지 블로킹된다. 클라이언트 SDK 쪽에서는 그냥 `onPermissionRequest` 콜백 하나만 등록해두면 두 프로바이더 모두에 대해 똑같이 동작한다. 거부하면 도구 호출이 실패 응답으로 떨어지고, 승인하면 진행된다. 안전하게 쓸 도구(Read, Glob, Grep 등)는 자동 허용 리스트에 넣어놓는다든지 그런식의 운용도 가능하다.

## 런타임 컨트롤

세션을 죽이지 않고도 모델을 바꾸거나 (`agent.set-model`), 권한 모드를 토글하거나 (`agent.set-permission-mode`), 진행 중인 턴을 인터럽트하거나 (`agent.interrupt`), 프로세스를 죽였다 같은 설정으로 다시 띄울 수 있다 (`agent.restart`). Resume도 마찬가지로 DB에 정규화된 메세지를 JSONL로 변환해서 `--resume`으로 클로드 코드에 다시 먹이면, 끊긴 자리에서 컨텍스트를 그대로 이어간다. 코덱스 쪽에도 그에 맞게 thread/resume API에 맞게 변환해서 넣어준다.

## One-shot Completion

세션 띄우는 게 무거울 때, 짧은 추론 한 번이면 충분한 작업용으로 `completion()` API를 따로 뺐다. 프롬프트 던지고 결과/토큰/비용/소요시간 받고 끝. Loom에서 대화가 시작될때, 대화방의 이름을 한번 정하는데 이걸 이용한다. 멀티턴 세션이 필요 없으니 이쪽이 훨씬 가볍고 빠르다.

## 이벤트 종류

WebSocket으로 흘러나오는 이벤트는 12종류다. `init`(세션 초기화) / `thinking`·`thinking_delta`(추론) / `assistant`·`assistant_delta`(응답, 스트리밍 청크 포함) / `tool_use`·`tool_result`(도구 호출/결과) / `permission_needed` / `milestone`(스킬 진척) / `user_message`(멀티 클라이언트 동기화) / `interrupted` / `error` / `complete`.

`_delta` 계열은 토큰 스트리밍이라 ChatGPT처럼 한 글자씩 흐르는 UI를 그대로 만들 수 있고, `milestone`은 스킬 안에서 `dispatch.milestone("5건 추출 완료")` 같이 명시적으로 쏘는 진척 신호다. UI 쪽 useAgent 훅은 이 모든 타입을 콜백으로 분기해 받을 수 있다.

## React 패키지

`@sna/react`는 위 client를 React에 묶은 훅 묶음 + 그냥 갖다 붙이면 되는 채팅 UI다.

- 훅: `useAgent`, `useSessionManager`, `useSkillEvents`, `useSnaClient`, `useResponsiveChat`
- 컴포넌트: `<SnaProvider>`로 감싸고 `<ChatPanel>` 하나 떨어뜨리면 메세지 버블, 도구 호출 카드, thinking 접힘 박스, 마크다운 렌더, 퍼미션 다이얼로그가 다 들어있는 채팅이 그대로 뜬다

처음에 Electron + Xterm으로 클로드 코드를 띄웠던 그 시절을 생각하면, 이젠 백엔드는 SNA 서버에 맡기고 프론트는 진짜 평범한 React 앱처럼 짜면 된다는 게 가장 큰 차이다.

## Mock 서버 (@sna/testing)

그리고 이걸 만들면서 LLM콜을 진짜 무진장 많이 돌렸는데, 진짜 클로드 토큰을 태우면서 통합 테스트 돌리면 금방 가난해진다. 그래서 Anthropic Messages API와 시그니처가 동일한 모킹 서버를 SDK 안에 같이 넣어놨다. `ANTHROPIC_BASE_URL`만 모킹 서버로 갈아끼우면 클로드 코드 본인은 자기가 진짜 Anthropic에 붙는 줄 안다. CI에서 SNA 풀스택을 검증할 때, 그리고 SDK 자체 테스트에서 쓴다. `sna tu claude` 한 줄이면 격리된 환경에서 모킹 API에 연결된 클로드가 그대로 뜬다. 동작방식이 좀 웃기기는 하다. 유저가 보낸 메세지를 그대로 뒤집어서 응답한다.
유저: "안녕, 밥 먹었니?" -> 에이전트 "?니었먹 밥 ,녕안"

## Electron / Node 런처

SNA 서버는 별도 노드 프로세스로 띄우는 게 정석인데, Electron에 임베드하려면 asar 패키징, better-sqlite3 네이티브 바이너리 경로, ready 감지 같은 것들이 골치 아프다. 그래서 `startSnaServer({ port, dbPath, ... })` 한 줄로 fork → 환경변수 셋업 → 네이티브 바인딩 자동 탐지 → 준비 완료까지 다 해주는 런처를 따로 노출시켜놨다. Next.js나 Vite 같은 일반 Node 환경용 버전도 동일한 옵션 시그니처로 둘 다 있다.

---

# 그래서 그 영업 친구는

여기까지 만들다보니까.. 영업 친구를 까맣게 잊고 있었다.

처음엔 `/scrape` `/get-form` `/fill-form` `/send` 네 개의 슬래시 커맨드를 한 터미널에 차례로 두드리는 단순한 자동화였는데, 그 과정에서 마음에 안 드는 구석을 하나하나 파다 보니 영업 자동화 앱은 사라지고 그 위에 영업 자동화 앱을 짤 수 있는 베이스 라이브러리가 남았다. 그러다가 Loom을 개발하고 앉아있다. 본말이 전도된 셈이지만, 어쨌든 이제는 30건이든 100건이든 동시 송신이 가능하고, 진척은 폴링이 아니라 WebSocket으로 실시간이고, 도중에 모델을 바꿀 수도 있고, 이력은 프로바이더에 귀속되지 않는다.

Loom이 끝나면 그 영업 자동화 앱을 다시 짜도록 하겠다. 이번엔 SNA 위에서. 이 친구의 본래 워크플로우가 SNA위에서 돌아가도록...

순수한 마음으로 시작해서 한참 돌아왔지만, 결국 출발점으로 다시 돌아오는 길에 라이브러리 한 개가 생겨버린 그런 프로젝트. 개발자라면 누구나 한 번쯤 빠져봤을 드릴다운이긴 한데, 어쨌든 결과물은 남았다. 그 친구한테 자랑해야겠다.

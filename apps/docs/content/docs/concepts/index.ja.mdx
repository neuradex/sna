---
title: 概要
description: SNA ランタイムの形。サーバ、session manager、providers、履歴。
icon: LayoutGrid
---

SNA はセッションごとに長期稼働プロセスを持つ Hono HTTP/WebSocket
サーバだ。サーバはプロバイダ非依存。Claude Code、Codex、OpenCode は
すべて共通の `AgentProvider` インタフェースを実装するので、1 つの
セッションが文脈を失わずにランタイムを切り替えられる。

```
SnaClient
   │   HTTP (request/response, ordering)
   │   WS   (live events, fanout)
   ▼
Hono server  ──►  SessionManager
                       │
                       ├─ Session (per session id)
                       │     ├─ AgentProcess  ◄── spawn(provider, cwd, ...)
                       │     ├─ Event buffer (in-memory, cursor-keyed)
                       │     └─ RuntimeSession chain (config history)
                       │
                       └─ Canonical history (SQLite)
```

## 読む順序

<Cards>
  <Card title="セッション" href="/docs/concepts/sessions" description="ライフサイクル、RuntimeSession チェーン、PATCH 意味論。" />
  <Card title="正規化履歴" href="/docs/concepts/history" description="フラットブロック、プロバイダネイティブ再構築。" />
  <Card title="プロバイダ" href="/docs/concepts/providers" description="AgentProvider インタフェースと runtime pool。" />
  <Card title="権限" href="/docs/concepts/permissions" description="プロバイダ横断の統一権限フロー。" />
</Cards>

ワイヤプロトコルの詳細 (HTTP ルート、WebSocket op、イベントタイプ)
は **[API Spec](/docs/api)** を参照。

---
title: 개요
description: SNA 런타임의 모양. 서버, session manager, providers, 히스토리.
icon: LayoutGrid
---

SNA는 세션마다 장기 실행 프로세스를 소유하는 Hono HTTP/WebSocket
서버다. 서버는 프로바이더 비종속적이다. Claude Code, Codex, OpenCode
가 모두 공통의 `AgentProvider` 인터페이스를 구현하므로 하나의 세션이
맥락을 잃지 않고 런타임을 갈아탈 수 있다.

```
SnaClient
   │   HTTP (request/response, ordering)
   │   WS   (live events, fanout)
   ▼
Hono server  ──►  SessionManager
                       │
                       ├─ Session (per session id)
                       │     ├─ AgentProcess  ◄── spawn(provider, cwd, ...)
                       │     ├─ Event buffer (in-memory, cursor-keyed)
                       │     └─ RuntimeSession chain (config history)
                       │
                       └─ Canonical history (SQLite)
```

## 읽는 순서

<Cards>
  <Card title="Sessions" href="/docs/concepts/sessions" description="라이프사이클, RuntimeSession 체인, PATCH 의미론." />
  <Card title="정규화 히스토리" href="/docs/concepts/history" description="플랫 블록, 프로바이더 네이티브 재구성." />
  <Card title="Providers" href="/docs/concepts/providers" description="AgentProvider 인터페이스와 runtime pool." />
  <Card title="권한" href="/docs/concepts/permissions" description="프로바이더 전체에 걸친 통합 권한 흐름." />
</Cards>

와이어 프로토콜 디테일(HTTP 라우트, WebSocket op, 이벤트 타입)은
**[API Spec](/docs/api)** 참고.

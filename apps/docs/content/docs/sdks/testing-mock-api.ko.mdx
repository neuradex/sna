---
title: "Testing Mock API"
description: "startMockAnthropicServer, runOneshot, MockServer의 사용법."
---

# Testing Mock API

## `startMockAnthropicServer(options?)`

**Signature**

```ts
const server = await startMockAnthropicServer(options?: { port?: number }): Promise<MockServer>
```

**Returns**

```ts
type MockServer = {
  url: string;
  port: number;
  logs: MockLogEntry[];
  close: () => Promise<void>;
};
```

**Log shape**

```ts
type MockLogEntry = {
  request: unknown;
  response: unknown;
  timestamp: number;
};
```

**Example**

```ts
import { startMockAnthropicServer } from "@sna-sdk/testing";

const mock = await startMockAnthropicServer();
process.env.ANTHROPIC_BASE_URL = mock.url;

try {
  // Anthropic-compatible API를 기대하는 코드를 실행합니다.
} finally {
  await mock.close();
}
```

**Behavior**

- `/v1/messages` endpoint를 제공합니다.
- 일반 user text는 deterministic response로 처리됩니다.
- tools가 선언되어 있고 prompt에 `[tool:Name]`이 있으면 tool_use event를 만들 수 있습니다.

## `runOneshot(options)`

**Signature**

```ts
await runOneshot(options: Record<string, unknown>): Promise<unknown>
```

**Use it when** 테스트에서 CLI 전체를 띄우지 않고 one-shot execution path만 검증하고 싶을 때 사용합니다.

## Instance helpers

### `generateInstanceName()`

고유한 test instance 이름을 만듭니다. 병렬 테스트에서 log/config directory 충돌을 피할 때 사용합니다.

### `getInstancesDir()` / `getInstanceDir(name)`

testing CLI가 사용하는 `.sna/instances` storage path를 계산합니다. Path를 직접 하드코딩하지 말고 이 helper를 사용하십시오.

### `listInstances()`

디스크에 저장된 test instance metadata 목록을 반환합니다. Debug UI나 cleanup script에서 사용합니다.

### `readInstanceMeta(name)` / `writeInstanceMeta(name, meta)`

test instance metadata를 읽고 씁니다. 추가 상태를 instance에 연결해야 할 때 사용합니다.

### `removeInstance(name)`

instance directory와 log를 제거합니다. 테스트 cleanup에서 사용합니다.

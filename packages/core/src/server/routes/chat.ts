/**
 * @deprecated The HTTP chat routes now live in `routes/openapi.ts` and are
 * mounted automatically by `createSnaApp()`. This module is kept as an
 * empty shim so the `@sna-sdk/core/server/routes/chat` subpath export
 * keeps resolving for consumers; it intentionally exports nothing.
 */
export {};

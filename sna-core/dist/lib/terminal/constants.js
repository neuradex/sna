const TERMINAL_WS_PORT = 3001;
const TERMINAL_WS_URL = `ws://localhost:${TERMINAL_WS_PORT}`;
const TERMINAL_MIN_WIDTH = 300;
const TERMINAL_MAX_WIDTH_RATIO = 0.6;
const TERMINAL_DEFAULT_WIDTH = 480;
export {
  TERMINAL_DEFAULT_WIDTH,
  TERMINAL_MAX_WIDTH_RATIO,
  TERMINAL_MIN_WIDTH,
  TERMINAL_WS_PORT,
  TERMINAL_WS_URL
};

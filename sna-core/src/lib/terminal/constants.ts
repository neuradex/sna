export const TERMINAL_WS_PORT = 3001;
export const TERMINAL_WS_URL = `ws://localhost:${TERMINAL_WS_PORT}`;

// Bottom drawer dimensions
export const TERMINAL_BAR_HEIGHT = 36;       // collapsed bar height (px)
export const TERMINAL_DEFAULT_HEIGHT = 340;  // default open height (px)
export const TERMINAL_MIN_HEIGHT = 120;      // minimum open height before snap-close
export const TERMINAL_SNAP_THRESHOLD = 80;   // drag below this → snap closed
export const TERMINAL_MAX_HEIGHT_RATIO = 0.85;
